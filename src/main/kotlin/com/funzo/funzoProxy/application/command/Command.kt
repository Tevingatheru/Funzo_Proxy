package com.funzo.funzoProxy.application.command

/**
 * Marker interface representing a command. A command is an object that encapsulates a single task or action
 * that needs to be performed.
 * @param R The return type of the command.
 */
interface Command<R> {
}